/*
  main.ino - Template minimo per progetto Arduino
  ------------------------------------------------
  Questo file contiene la struttura base di uno sketch:
  - setup(): eseguito una sola volta all'avvio/reset
  - loop(): eseguito continuamente
*/

void setup() {
  // Inizializza qui periferiche e pin.
  // Esempio: Serial.begin(9600);
}

void loop() {
  // Inserisci qui la logica principale del progetto.
  // Evita delay lunghi quando possibile per mantenere reattività.
}
